#include<bits/stdc++.h>

using namespace std;

int main () {
	ios_base::sync_with_stdio(false);

	double l, i, x;
	
	while (cin >> l and l) {
		i = l+l-3;
		x = (i-l)/l;
		cout << fixed << setprecision(6) << x << "\n";
	}
	
	return 0;
}
