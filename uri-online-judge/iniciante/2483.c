#include<stdio.h>
#include<stdlib.h>
#include<math.h>
#define pi 3.14159

int main () {
    long long int n, i;

    while (scanf("%lld", &n) != EOF) {
        printf("Feliz nat");
        for (i=0; i<n; i++)
            printf("a");
        printf("l!\n");
    }

    return 0;
}
